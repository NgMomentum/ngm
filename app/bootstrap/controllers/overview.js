
msos.provide("app.bootstrap.controllers.overview");

app.bootstrap.controllers.overview.version = new msos.set_version(16, 5, 4);


angular.module(
    'app.bootstrap.controllers.overview', []
).controller(
    'app.bootstrap.controllers.overview.ctrl',
    [
        '$scope',
        function ($scope) {
            "use strict";

        }
    ]
);